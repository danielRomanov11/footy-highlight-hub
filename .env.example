NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
YOUTUBE_API_KEY=
CRON_SECRET=

# Hours between ingest runs on site visits. 0 = every visit (default).
INGEST_INTERVAL_HOURS=0

# Set to "true" to enable Full Matches tab in the UI (Phase 4)
NEXT_PUBLIC_ENABLE_FULL_MATCHES=false
